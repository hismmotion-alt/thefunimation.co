# Static directory indexes (services/index.html, work/index.html, etc.)
# give Netlify clean trailing-slash URLs without extra redirects.

[[edge_functions]]
  path = "/*"
  function = "country-block"
